[pytest]
pythonpath = .
testpaths =
    tests
addopts = -ra --strict-markers --html=reports/pytest-report.html --self-contained-html
markers =
    framework: fast framework-level tests
    ui: browser-based UI tests
